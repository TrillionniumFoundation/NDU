# Supplemental exports

This directory contains convenience copies of the figures and compact summary CSV files that were present at the source directory root. They are not required by the canonical lightweight checker; the complete reproducibility inputs and audit outputs are under `../reproducibility/`.

