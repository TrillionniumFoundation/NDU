# R25 current argument and preserved derivation lineage

The controlling report is R24 at review commit `9080e29443191f9bb415ab0a213af446d4ce3be3`; its scientific object was `63bbb843e1bbe7cac2170dc7d317d4cf18bb2bb8`.

## Current journal-facing argument

`main.tex`/`main.pdf` state the exit-review institution, optimized restriction-release frontier, comparator-adjusted rents, sufficient promised-payment recursion, implemented certification, and new common-instance evidence. `electronic_companion.tex`/`.pdf` supply the extended stock-identification, transfer, release-path, degeneracy, example, dual-bound, cache, outer-envelope, and design details. Their theorem chain is self-contained; the archive is not an additional required formal companion.

## Preserved foundations

The complete reviewed R24 main/EC source and PDFs, computational record, historical supplement, bibliography, README, and checklist are in `predecessor/`. All have exact SHA-256 hashes in `PREDECESSOR_SHA256.json`. Existing earlier revision paths remain unchanged and retain their own derivations and provenance. Some R24 documents input material from earlier revision directories; those dependencies are retained at exactly their prior repository paths, so preservation is not limited to a list of displayed theorem environments in the top-level file.

The earlier physical service/stock-identification and value-gradient bridge establish the retained model foundations and implementation interpretation. General accepted balance covers vector services, signed payments, boundary points, and additional polyhedral commitments. R24's benchmark-relative transfer bijection handles optimized comparators with slack. R25 retains these statements and then derives a value frontier that is not the bijection itself.

The earlier constant-comparator sharp-friction result remains valid under its original assumptions. R25's reoptimized-comparator result handles a different economic object and gives a different threshold set; the archived theorem is not silently generalized or deleted.

## Preserved implementation and unfavorable evidence

The root `computational_supplement.tex`/`.pdf` and `historical_supplement.tex`/`.pdf` remain their R24-era documents. They retain value-gradient and neural variants, multiple repair/certificate generations, all earlier numerical comparisons, and the complete phase-resolved timing record. They are archival documents, not current R25 performance claims.

The R24 strict-target learned methods fall back in all measured cases and have negative paired savings against lifted continuation. The analysis code was already correct; the earlier manuscript's amortization sign is corrected in the new main paper. The predecessor source is preserved verbatim, including that documented historical error, so its presence in `predecessor/` must not be confused with the current formula.

The R24 robust costs compare different achieved gains. Their raw quality–time coordinates remain available; R25 does not claim matched-quality dominance or an unmeasured Pareto frontier. The new fixed-geometry cache is not retroactively inserted into those costs.

## New versus inherited evidence

Only the files in R25 `results/` labeled hierarchy, radius, cache, structural, promise, and their replay/analysis are new scientific computations. Historical replay files certify unchanged inherited data. The equality-aware successive outer refinement is a proved option, not the source of the fine-radius numbers. Exact finite-action recursion checks do not establish exactness for an arbitrary continuous promise grid. All families are designed, not calibrated from field observations.
