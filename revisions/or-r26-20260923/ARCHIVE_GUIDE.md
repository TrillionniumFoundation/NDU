# R26 derivation and evidence map

## Current argument

Read the root main paper and formal electronic companion. The optimized restriction-release theorem, capacity/friction rent results, transfer geometry, stock identification, primal–dual certificate, reusable comparator bound, and local robust-comparator result are inherited from R25 and kept intact. The new material is `continuous_main.tex` and `continuous_ec.tex`, integrated into main Section 6 and EC.10, plus the new continuous-state table.

## Sources actually used

The two R24 reports are distinguished in `SOURCE_REVIEWS.json`. R25's main/EC, response, exact examples, promise checks, evidence, and archive guide supply the latest scientific base. The R24 predecessor source supplies the slack-aware transfer derivation, general balance, switching-tension and constant-comparator distinctions. The computational and historical supplements retain the NDU/value-gradient bridge, learning, repair generations, full cost accounting, and older institution. Nothing in R26 retrospectively rewrites their assumptions or measurements.

## Complete preservation

`predecessor/` contains 12 R25 files copied byte for byte from scientific base `7f3f12c9d412b45a570725dc9b61203d64da5e41`; `PREDECESSOR_SHA256.json` verifies them. The R25 predecessor subdirectory already preserves R24. Existing revision/review directories, root computational and historical supplements, old labels and references remain unchanged. The package checker additionally matches every R25 theorem, proposition, lemma, corollary, assumption, remark, and proof environment against the current source. The old amortization prose error remains in the historical R24 copy, not in the current main equation.

## Evidence boundaries

`envelope.py` creates new continuous-tier synthetic models; `replay.py` independently verifies exact rational witnesses. This is not finite-action enumeration, field data, neural fitting, or a timing comparison. The six designs validate whole-domain certificate widths through the proved conjugate and triangle inequalities. The number of anchors for an arbitrary target error is not bounded by these tests. Stored timings are proposal-accounting metadata only.

`results/inherited_r25_replay.json` and `inherited_r24_replay.json` recheck old results without regenerating or reattributing them. The final-SHA job reruns these checks on the actual published commit. `MANIFEST.json` pins the new sources, PDFs, response, and evidence. `manuscript.patch` reproduces the published root text sources from the pinned R25 predecessor; the final manifest separately pins those actual sources and PDFs.
