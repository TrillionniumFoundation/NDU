\begin{table}[p]
\caption{Contractual structure and inherited algorithmic machinery.}\label{tab:r37-priority}
\begin{tabular}{p{.24\textwidth}p{.69\textwidth}}
\toprule
Result in this article & Contribution and precise comparison \\
\midrule
Deterministic cells and pathwise participation & Theorem~\ref{thm:frontier} gives the contractual cell cost and ordered sufficiency. Theorem~\ref{thm:r34-pathwise} proves that even private pathwise randomization cannot improve it; the segmentation recurrence itself is standard. \\
Global continuous codebooks & Lemma~\ref{lem:r34-anchor} and Theorem~\ref{thm:r34-global} establish cap anchoring except at the highest level and optimize that level exactly. These are not consequences of generic matrix search. Proposition~\ref{prop:r34-offcap} rules out cap-only discretization. \\
Terminal costs after minimization & Lemma~\ref{lem:r37-cost} proves a contractual single-crossing identity with cancellation of heterogeneous intermediate curvatures. It implies Monge structure after continuous interval minimization, which does not follow merely from separate convexity. \\
Completed row-minimum search & Lemmas~\ref{lem:r37-suffix}--\ref{lem:r37-prefix} audit the exact keys in the classical easy-direction staircase case described by \citet{Chan2021}. SMAWK is due to \citet{AggarwalEtAl1987}; generalized partial search and partial-query structures have their own literature \citep{KlaweKleitman1990,Kaplan2017}. \\
Complete linear-work frontier & Theorem~\ref{thm:r37-linear} combines the contractual oracle and reachability with classical search. It is not a new general partial-Monge algorithm. \citet{Emmerich2026} illustrates the same broad Monge-DP acceleration pattern in another application. \\
Broader exact representation & The companion preserves the compact-response, tight-size, piecewise-quadratic, and exact-memory theorems. Laminar allocation, memoization, and behavioral minimization are explicitly attributed rather than relabeled. \\
\bottomrule
\end{tabular}
\end{table}

\begin{table}[p]
\caption{Accounting for Theorem~\ref{thm:r37-linear}; ordered rational input, $1\leq m\leq k$.}\label{tab:r37-accounting}
\begin{tabular}{p{.40\textwidth}p{.53\textwidth}}
\toprule
Cost component & Included work or storage \\
\midrule
Input validation and six prefix moments & $O(k)$ rational operations; $O(k)$ stored values. \\
Sorting & Input interface requires ordered distinct caps; otherwise add $O(k\log k)$ exact comparisons and reorder associated primitives. \\
Continuous terminal minimization & Included in $O(k)$ oracle requests; each interval minimizes its quadratic exactly, including singleton and endpoint cases. \\
Key comparisons and repeated requests & Included in $O(mk)$ work; a repeated finite request is charged again. No quadratic entry cache is assumed. \\
Padding comparisons & Included index comparisons; undefined economic costs are never evaluated. \\
Previous-layer values and backpointers & $O(mk)$ stored values and indices, plus $O(k)$ working search space. \\
Reconstruction and output & One winning codebook per budget; $O(m^2)\leq O(mk)$ symbols written. No reconstruction at every candidate endpoint. \\
Rational heights and integer operations & $O(S)$ bits per evaluated rational, $S=O(k[L+\log(k+2)])$; conservative $O(mkS^3)$ bit operations, including reduction and comparison. \\
Physical bit storage & $O((mk+k)[S+\log(k+2)])$ bits for the stated arrays and indices; not a claim of $O(mk)$ bits. \\
Inherited public graph and response compiler & Not invoked by the renewal algorithm; their read-only data, compilation, and query costs enter the separate implementation menu when used. \\
External solver preprocessing & Not part of the frontier theorem. Finite-network construction, floating solver work, and exact certification are measured separately. \\
\bottomrule
\end{tabular}
\end{table}
