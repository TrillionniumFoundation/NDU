\section{Current Computational Evidence}\label{sec:study56}
The current study evaluates the new heterogeneous resource implementation and exact lattice regime, and measures the search and proof costs of price paths and forced-command screening. It does not pool the preceding section's R54 timings with new executions. The primary protocol specifies 186 timed method runs. An extension inherited from post-inspection development adds 39 exact-target and encoding runs; nine untimed mechanical replays supply search diagnostics. The present revision freshly executes all 225 runs from a source snapshot committed before execution. Its cases, methods, limits, and tolerances match the recovered corrected design. These are new execution records, not a relabeling of unpublished local timings. All failures are retained, and every numerical statement below is generated from the new records. The companion documents the earlier publication failure and the distinction between recovered development and current evidence.

\subsection{Design and outcome definitions}
The primary comparison crosses three seeds, two optimization limits (0.5 and 2 seconds), and four model families. Their history, catalog, and budget dimensions are $(6,7,3)$, $(8,9,3)$, $(24,17,4)$, and $(96,25,4)$. The first family has common rewards and the others have heterogeneous rewards. Probabilities are uniform; caps, ceiling increments, service curvatures and reward parameters are drawn on declared rational grids. The promise is three quarters of aggregate capacity. Charges are nonnegative, and all numerical parameters and instance digests are saved before the corresponding method executes. The same model at different limits is a paired observation, not a new independent instance.

Each method runs in a fresh process with a two-GiB address-space limit and single-thread numerical libraries. The price and deficit methods request width $1/20$ in this panel. Enumeration requests exact completion. The direct support-indicator mixed-integer comparator uses numerical cost envelopes and has a separate evidence status. Setup, optimization, serialization and independent checking are timed separately; a twelve-second initial checker allowance is not part of the optimization allowance. Native-library overruns, failures and the last completed phase remain reported. Table~\ref{tab:primary56} gives all family-level medians and success counts.

\input{revisions/or-r57-deficit-publication-20260926/generated/current_outcomes.tex}
